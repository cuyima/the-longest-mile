name: Release

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Get version from package.json
        id: package_version
        run: echo "version=$(node -p "require('./package.json').version")" >> $GITHUB_OUTPUT

      - name: Update module.json version
        run: |
          # Update version in static/module.json using package.json version
          sed -i 's/"version": "[^"]*"/"version": "${{ steps.package_version.outputs.version }}"/' static/module.json
          
          # Update download URL in static/module.json
          sed -i 's|"download": "[^"]*"|"download": "https://github.com/${{ github.repository }}/releases/download/v${{ steps.package_version.outputs.version }}/module.zip"|' static/module.json

      - name: Build project
        run: npm run build

      - name: Create module.zip
        run: |
          cd dist
          zip -r ../module.zip .
          cd ..

      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref }}
          release_name: Release ${{ steps.package_version.outputs.version }}
          body: |
            ## What's Changed
            - Version ${{ steps.package_version.outputs.version }}
            
            **Full Changelog**: https://github.com/${{ github.repository }}/compare/v${{ steps.package_version.outputs.version }}...v${{ steps.package_version.outputs.version }}
          draft: false
          prerelease: false

      - name: Upload module.zip
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./module.zip
          asset_name: module.zip
          asset_content_type: application/zip

      - name: Upload module.json
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./dist/module.json
          asset_name: module.json
          asset_content_type: application/json